plugins {
    id 'io.micronaut.build.internal.r2dbc-testproject'
}

dependencies {
    implementation(mnSql.mssql.jdbc)
    implementation(libs.managed.r2dbc.mssql)
}

graalvmNative {
    binaries {
        main {
            buildArgs('-H:+AddAllCharsets')
        }
    }
}

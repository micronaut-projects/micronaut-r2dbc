CREATE TABLE pet(id BIGINT NOT NULL IDENTITY PRIMARY KEY, name VARCHAR(255), type VARCHAR(255), owner_id BIGINT);
CREATE TABLE owner(id BIGINT NOT NULL IDENTITY PRIMARY KEY, name VARCHAR(255), age INT);
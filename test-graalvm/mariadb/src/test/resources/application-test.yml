r2dbc:
  datasources:
    default:
      db-type: "mariadb"
      dialect: "MYSQL"
      schema-generate: "CREATE_DROP"
      protocol: "mariadb"
test-resources:
  containers:
    mariadb:
      image-name: "mariadb"

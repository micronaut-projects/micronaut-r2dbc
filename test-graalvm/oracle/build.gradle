plugins {
    id 'io.micronaut.build.internal.r2dbc-testproject'
}

dependencies {
    runtimeOnly(libs.managed.r2dbc.oracle)
    runtimeOnly(mnSql.ojdbc8)
    testImplementation(libs.testcontainers.oracle)
}
